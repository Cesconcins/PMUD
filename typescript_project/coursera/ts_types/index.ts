let age = 34 //If we don't specify the type we can assign whatever we want
let firstName: string = 1234 //If we assign a type but the value of the variable is a different type, we get an error
let busy: boolean = true //The value of busy can only be a boolen